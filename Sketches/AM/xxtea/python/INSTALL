In order to install the xxtea module you need to install
libxxtea first:
   * cd xxtea/libxxtea/src
   * make 

Then you build and install install the python bindings:
   * cd xxtea/python
   * python setup.py install

Anagha, June 2006

